
  /*
  function toggleDarkLight() {
    var myheader = document.getElementById("header");
    var currentClass = myheader.className;
    myheader.className = currentClass == "dark-mode" ? "light-mode" : "dark-mode";
  }
  */
